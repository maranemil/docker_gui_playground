https://gursimar27.medium.com/run-gui-applications-in-a-docker-container-ca625bad4638

docker-compose build
docker-compose up

Inside the Docker container
apt install -y xauth
$ xauth add <token>
$ xauth list

VNC server software to your container:

FROM ubuntu:latest
RUN apt-get update && apt-get install -y firefox x11vnc xvfb
RUN echo "exec firefox" > ~/.xinitrc && chmod +x ~/.xinitrc
CMD ["v11vnc", "-create", "-forever"]


docker run --rm -ti --net=host -e DISPLAY=:0 psharkey/eclipse